<VirtualHost *:9091>
    # ServerName app4laopo.com
    ServerAdmin lianggy0719@126.com

    # Make sure pathes are correct
    WSGIDaemonProcess app_reimbursement user=ubuntu group=ubuntu python-home=/home/ubuntu/app4laopo/app_reimbursement/venv threads=4
    WSGIScriptAlias / /var/www/app4laopo/app_reimbursement.wsgi

    <Directory /var/www/app4laopo>
        WSGIProcessGroup app_reimbursement
        WSGIApplicationGroup %{GLOBAL}
        Order deny,allow
        Allow from all
    </Directory>
</VirtualHost>

# vim: syntax=apache ts=4 sw=4 sts=4 sr noet
